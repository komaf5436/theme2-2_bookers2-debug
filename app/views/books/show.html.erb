<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info', user: @book.user %>
      <!--応用2-20 _info.html.erbで未定義になっており、引数を渡さないといけないため部分テンプレート内のローカル変数をuserとし、コントローラーで定義した@book.userを入れる-->
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @book_new %>
      <!--応用2-22 新規に作った@book_new変数に変更-->
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>Book detail</h2>
  		<table class='table'>
  		  <tr>
          <td><%= link_to(@book.user) do %>
            <%= image_tag @book.user.get_profile_image, size:"100x100" %><br>
            <%= @book.user.name %>
            <% end %>
          </td>
          <td><%= link_to @book.title, @book %></td>
          <td><%= @book.body %></td>
          <% if @user == current_user %>
            <td><%= link_to 'Edit', edit_book_path(@book.id), class: "btn btn-sm btn-success edit_book_#{@book.id}" %></td>
            <td><%= link_to 'Destroy', book_path(@book.id), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_book_#{@book.id}"%></td>
          <% end %>
          <!--応用2-28 他ユーザーが投稿した本の編集、削除のリンクを表示させないためif文を挿入-->
          <!--if ~~==current_userは投稿したユーザーとログインしているユーザーが一致していたらという意味-->
        </tr>
      </table>
    </div>
  </div>
</div>